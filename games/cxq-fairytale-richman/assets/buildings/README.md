AI-generated building sprites used by the three CxQ maps.

Each PNG/WebP source is an independently generated transparent asset, not a crop from a sprite sheet. Runtime uses the optimized WebP files.
